#!/usr/bin/env zsh
# ==============================================================================
# Conda 别名 - 精简实用版
# 只保留最常用、最有价值的别名，鼓励使用原生命令
# ==============================================================================

# ------------------------------------------------------------------------------
# 核心别名 - 只保留最常用的
# ------------------------------------------------------------------------------

# 环境相关（最高频使用）
alias cenv='conda env list'                    # 查看环境列表
alias cact='conda activate'                    # 激活环境（保持接近原命令）

# 包管理（日常使用）
alias clist='conda list'                       # 列出包
alias cinfo='conda info'                       # 系统信息

# 清理（实用性强）
alias cclean='conda clean -a'                  # 清理所有缓存

# ------------------------------------------------------------------------------
# 说明
# ------------------------------------------------------------------------------
# 其他命令保持使用原生形式：
# - conda create -n myenv python=3.10
# - conda install package
# - conda update package  
# - conda remove package
# - conda deactivate
# 
# 这样可以：
# 1. 保持对原生命令的熟悉
# 2. 在其他环境中也能正常工作
# 3. 减少记忆负担